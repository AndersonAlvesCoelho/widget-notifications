import { Widget } from "@/components";

export default function Home() {
  return (
   <main className="h-screen bg-zinc-50 flex items-center justify-center text-zinc-800 dark:text-zinc-50 dark:bg-black">
     <Widget />
   </main>
  )
}
